version=v0.1.1

# 可选：覆盖 ~/.config/filo/signing/identity.txt 中的固定签名身份。
# 名称或 SHA-1 指纹均可；留空时读取上述文件。
# 从 security find-identity -v -p codesigning 的结果中选择。
# macOS 上两处均未配置或填 - 时，make dev 拒绝启动未固定签名的应用。
APPLE_SIGNING_IDENTITY=
